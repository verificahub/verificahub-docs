# ai/ — shared project context

Tracked, shareable context for AI-assisted work on this repo.

- `index.json` — pointers to key files and operating assumptions.
- `context.md` — concise project memory for recurring tasks.

Ephemeral, per-machine cache (embeddings, indexes, session notes) goes in `.ai/`,
which is git-ignored.
